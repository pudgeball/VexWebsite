<h1>Listing <%= t("activerecord.attributes.school.title").pluralize %></h1>

<table>
  <tr>
    <th><%= t("activerecord.attributes.school.region") %></th>
    <th><%= t("activerecord.attributes.school.name") %></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @schools.each do |school| %>
  <tr>
    <td><%= school.region %></td>
    <td><%= school.name %></td>
    <td><%= link_to 'Show', school %></td>
    <td><%= link_to 'Edit', edit_school_path(school) %></td>
    <td><%= link_to 'Destroy', school, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New School', new_school_path %>
