require 'test_helper'

class EventRecordTest < ActiveSupport::TestCase
#  test "EventRecord attributes must not be empty" do
#    record = EventRecord.new
#    assert record.invalid?
#    assert record.errors[:loss].any?
#    assert record.errors[:tie].any?
#    assert record.errors[:win].any?
#    assert record.errors[:event_id].any?
#    assert record.errors[:team_id].any?
#  end
#  
#  test "loss must be greater than or equal to zero" do
#    record = EventRecord.new(tie: 0, win: 0, :event_id:)
#    record.loss = -1
#    assert record.invalid?
#  end
end
